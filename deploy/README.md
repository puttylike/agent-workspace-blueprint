# Deployment example

The unit in this directory is documentation only. It is not installed by the
project. Review filesystem permissions, private configuration, service user,
network exposure, and authentication before deployment. The application must
remain bound to `127.0.0.1:3001`.

